//utilizando a estrutura while
import 'dart:io';

void main() {
  String? entrada = stdin.readLineSync();
  int contador = int.parse(entrada!);
  while (contador <= contador) {
    if (contador % 2 == 0) {
      print(contador);
    }
    contador++;
  }
}
